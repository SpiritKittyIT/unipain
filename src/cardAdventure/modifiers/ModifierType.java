package cardAdventure.modifiers;

/**
* enum to represent the modifier type of the Card class.
*/
public enum ModifierType {
    /**
    * When the modifier effect is triggered when attacking.
    */
    ATTACK,
    /**
    * When the modifier effect is triggered when defending.
    */
    DEFENCE
}
