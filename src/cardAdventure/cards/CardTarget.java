package cardAdventure.cards;

/**
* enum to represent the target type of the Card class.
*/
public enum CardTarget {
    /**
    * When the cards effect targets the opponent.
    */
    OPPONENT,
    /**
    * When the cards effect targets the user.
    */
    SELF
}
